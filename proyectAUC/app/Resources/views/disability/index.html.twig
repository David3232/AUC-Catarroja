{% extends 'base.html.twig' %}

{% block body %}
    <h1>Disabilities list</h1>

    <table>
        <thead>
            <tr>
                <th>Iddi</th>
                <th>Name</th>
                <th>Description</th>
                <th>Grade</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for disability in disabilities %}
            <tr>
                <td><a href="{{ path('disability_show', { 'iddi': disability.iddi }) }}">{{ disability.iddi }}</a></td>
                <td>{{ disability.name }}</td>
                <td>{{ disability.description }}</td>
                <td>{{ disability.grade }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('disability_show', { 'iddi': disability.iddi }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('disability_edit', { 'iddi': disability.iddi }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('disability_new') }}">Create a new disability</a>
        </li>
    </ul>
{% endblock %}
